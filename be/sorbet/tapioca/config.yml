gem:
  # Add your `gem` command parameters here:
  #
  exclude:
  - cgi  # typed: false to suppress stdlib conflict (unescape/unescapeURIComponent arity mismatch)
  # doc: true
  # workers: 5
dsl:
  # Add your `dsl` command parameters here:
  #
  # exclude:
  # - SomeGeneratorName
  # workers: 5
